/**
 * The delay both flashcard implementations (Unseen, Modules) wait after
 * marking a card before auto-advancing to the next one, matching the
 * original standalone pages.
 */
export const FLASHCARD_AUTO_ADVANCE_MS = 250
