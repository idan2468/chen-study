/**
 * Every route path in the app, in one place, so `<Route path>`, `<Link to>`
 * and any redirect can't drift apart from a typo.
 */
export const APP_ROUTES = {
  home: "/",
  unseen: "/unseen",
  modules: "/modules",
} as const
